<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="BBS_FR">
	<select id="count" resultType="int">
			select count(*) from BBS_FR
	</select>
	
	<select id="list" parameterType="map" resultType="FR">		 
		select * from 
		(select rownum rnum, FR_NO, USER_ID, FR_SUBJECT,
		  FR_CONTENT, FR_FILE, FR_ORIGINAL, 
		  FR_READCOUNT, FR_DATE
		  from
		  (select * from BBS_FR
		   order by FR_DATE desc
		  )
		)
	  where
	  	<![CDATA[
	  		rnum >= #{start} 
	  	 AND
	  	 	rnum <= #{end} 
	  	]]>
	</select>
	
	<insert id="insert" parameterType="FR">
	<selectKey resultType="int" order="BEFORE"
					keyProperty="FR_NO">
			select nvl(max(FR_NO),0)+1 from BBS_FR
		</selectKey>
		insert into BBS_FR
		(FR_NO, 
		USER_ID, FR_PASS, FR_CSFC,
		FR_SUBJECT, FR_CONTENT, FR_DATE,
		FR_READCOUNT, FR_FILE, 
		FR_ORIGINAL)
		values
		(#{FR_NO},
		#{USER_ID}, #{FR_PASS}, #{FR_CSFC},
		#{FR_SUBJECT}, #{FR_CONTENT},
		sysdate, #{FR_READCOUNT},
		#{FR_FILE, jdbcType=VARCHAR},
		#{FR_ORIGINAL, jdbcType=VARCHAR}
		)
	</insert>
	
</mapper>